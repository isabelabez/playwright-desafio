[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "playwright_desafio"
version = "0.1.0"
dependencies = [
  "pytest",
  "pytest-playwright",
  "playwright",
]

[tool.setuptools.packages.find]
where = ["src"]